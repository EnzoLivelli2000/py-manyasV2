import 'package:flutter/material.dart';

class PruebaStorie extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('acá iran las stories', textAlign: TextAlign.center,style: TextStyle(color: Colors.black),),
    );
  }
}
